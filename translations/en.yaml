configuration:
  device_home_assistant_url:
    name: Home Assistant URL
    description: Required Home Assistant URL embedded into firmware for device-side API access.
  device_home_assistant_token:
    name: Device Home Assistant Token
    description: Required long-lived access token paired with the device Home Assistant URL.
network:
  8099/TCP: Direct web UI port
